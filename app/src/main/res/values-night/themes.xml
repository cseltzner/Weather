<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Weather" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/surface_night</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/brown_200</item>
        <item name="colorSecondaryVariant">@color/brown_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryDark</item>
        <!-- Customize your theme here. -->
        <item name="background">@color/black</item>
        <item name="colorSurface">@color/surface_night</item>
        <item name="colorPrimaryDark">@color/surface_night</item>
        <item name="colorAccent">@color/icon_night</item>
        <item name="android:actionMenuTextColor">@color/white</item>
        <item name="popupTheme">@style/ThemeOverlay.AppCompat.Dark</item>

        <item name="errorTextColor">@color/danger_red</item>
    </style>
</resources>